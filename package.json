{
  "dependencies": {
    "@material-ui/core": "^4.2.0"
  },
  "scripts":{
    "app" : "yarn server && yarn frontend",
    "frontend":"cd frontend && yarn start",
    "server":"cd backend && node server"
  }
}
